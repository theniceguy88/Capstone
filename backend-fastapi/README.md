# FastAPI Backend Placeholder

Build this after the SwiftUI MVP flow is working.

Planned responsibilities:

- User profile API
- Workout API
- Completed workout tracking
- Reward calculation
- Goal and streak logic
- Supabase database connection

Run later with:

```bash
uvicorn app.main:app --reload
```
